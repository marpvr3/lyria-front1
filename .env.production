# Variables de entorno locales (NO versionar).
# Solo variables prefijadas con `VITE_` se exponen al cliente.
# No almacenar secretos aquí (quedan visibles en el bundle del cliente).

# Solo esquema + host + puerto: las rutas ya incluyen `/api/v1/...`.
VITE_API_BASE_URL=https://bpojpsapp.duckdns.org:3011
